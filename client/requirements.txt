pillow
paho-mqtt
requests
numpy==1.26.4
selenium
openai
python-dotenv
opencv-python
pyaudio
librosa

# Decompression mode dependencies (required for advanced visual effects):
face-recognition
mediapipe
moderngl
imageio
imageio-ffmpeg
piper-tts
sounddevice
SpeechRecognition
openai-whisper

# Intent-based command handler (alternative to LLM):
transformers
torch

# Hardware-specific dependencies (not needed for simulation mode):
# 
# LED Hardware (Raspberry Pi only):
# pi5neo - WS2812 LED control library (requires spidev)
# Install on Pi: pip install pi5neo
# 
# Audio Hardware:
# PyAudio - real-time audio capture
# Install with: sudo apt install python3-pyaudio
# Or: pip install pyaudio
# 
# librosa - advanced audio analysis
# Install with: pip install librosa soundfile scipy
# 
# Notes for decompression mode dependencies:
# 
# face-recognition - advanced facial recognition and emotion detection
# Note: Requires dlib which may need compilation on some systems
# On Raspberry Pi: sudo apt install libopenblas-dev liblapack-dev && pip install face-recognition
#
# mediapipe - real-time hand and body tracking for gesture-driven visuals
# Install with: pip install mediapipe
#
# moderngl - GPU-accelerated shader rendering for reflection & refraction effects
# Install with: pip install moderngl

# Console UI
rich
psutil